/**
 * Pack&Play - ユーティリティ関数
 *
 * 注意: このファイルは後方互換性のために残されています。
 * すべてのユーティリティ関数は js/main.js で定義されています。
 *
 * main.jsで定義済みの関数:
 * - getUrlParam(param)
 * - getRelativePath(targetPath)
 * - debounce(func, wait)
 * - filterItems(items, query, fields)
 * - sortItems(items, field, order)
 * - paginate(items, page, perPage)
 * - copyToClipboard(text)
 * - validateForm(formId)
 * - confirmAction(message, onConfirm)
 *
 * 新しいコードでは、main.jsの関数を直接使用してください。
 */

// このファイルは空です - すべての関数は main.js で定義されています
